class Graph:
    def __init__(self):
        self.size = 0
    
    def bfs(self):
        pass
    
    def fulkerson(self, s, t):
        flow = self.size*[0]
        g_residual = self
        
        while(bfs):
            bottleneck = minimum_i[capacity[i]]